{
  "type": "module",
  "scripts": {
    "build": "esbuild --bundle --outdir=dist --format=esm app.js style.css",
    "serve": "serve"
  },
  "dependencies": {
    "@openlab/alembic": "file:../.."
  },
  "devDependencies": {
    "esbuild": "^0.14.23",
    "serve": "^13.0.2"
  }
}
