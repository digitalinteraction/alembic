{
  "name": "client",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build:css": "parcel-css --bundle --minify style.css -o dist/style.css",
    "build:js": "esbuild --bundle --minify app.js --outdir=dist",
    "build": "npm run build:css && npm run build:js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@parcel/css-cli": "^1.5.0",
    "esbuild": "^0.14.23"
  }
}
